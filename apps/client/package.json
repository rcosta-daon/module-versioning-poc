{
  "name": "@module-versioning-poc/client",
  "version": "1.3.4",
  "private": true,
  "scripts": {
    "predeploy": "nx build client",
    "deploy": "cross-env NODE_DEBUG=gh-pages gh-pages --dist dist"
  }
}
